spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/primedemo
    username: postgres
    password: root
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update 